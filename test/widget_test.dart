// Basic smoke test — verifies the app can be imported without errors.
// Full widget tests require a configured Firebase project and a running emulator.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — replace with real integration tests once Firebase emulator is set up', () {
    expect(true, isTrue);
  });
}
